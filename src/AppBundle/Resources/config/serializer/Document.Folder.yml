AppBundle\Document\Folder:
    exclusion_policy: none
    properties:
        id:
            groups: ['folders', 'folder', 'postFolder', 'company']
        name:
            groups: ['folders', 'folder', 'postFolder', 'postDoc', 'docs', 'doc', 'company']
        accesses:
            groups: ['folders', 'folder', 'postFolder', 'docs', 'doc']
        companies:
            groups: ['postFolder', 'postDoc']
        docs:
            groups: ['folder']
        createdBy:
            groups: ['folder', 'postFolder']
        createdAt:
            groups: ['folder', 'postFolder']
        updatedBy:
            groups: ['folder']
        updatedAt:
            groups: ['folder']